name: Auto Gemini Feed Analysis

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  run-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: Отримання коду з репозиторію
        uses: actions/checkout@v4

      - name: Встановлення Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Автоматичне створення конфігурації та встановлення залежностей
        run: |
          npm init -y
          npm pkg set type="module"
          npm install @supabase/supabase-js @google/genai

      - name: Запуск аналізу кормів
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: node analyze-gemini.js
